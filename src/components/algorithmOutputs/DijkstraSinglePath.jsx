import React, { useEffect } from 'react';

const DijkstraSinglePath = ({ response }) => {
    useEffect(() => {
        console.log(response);
    }, []);
    
    return (
        <p>Test</p>
    );
}

export default DijkstraSinglePath;
